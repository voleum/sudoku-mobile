# 2.2.4 Схема базы данных

## Статус: ✅ Завершено
**Дата начала**: 19 сентября 2025
**Дата завершения**: 19 сентября 2025
**Ответственный**: voleum

---

## Общая архитектура хранения данных

### 🎯 Принципы проектирования БД

**Основные принципы:**
1. **Offline-First** - приоритет локального хранения
2. **Performance** - быстрые запросы через индексы
3. **Scalability** - готовность к росту данных
4. **Data Integrity** - ACID транзакции SQLite
5. **Privacy** - локальное хранение личных данных
6. **Backup Ready** - простое резервное копирование

**Стратегия разделения данных:**
- **SQLite** - структурированные игровые данные
- **AsyncStorage** - настройки и конфигурация
- **MMKV** - кэш и временные данные

---

## 📊 Архитектура хранилищ

```
┌─────────────────────────────────────────────────────────────────────┐
│                        DATA STORAGE ARCHITECTURE                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                      SQLITE DATABASE                       │    │
│  │                     (sudoku_game.db)                       │    │
│  │  ┌───────────────┬─────────────┬─────────────────────────┐  │    │
│  │  │     Table     │    Purpose  │       Size Est.         │  │    │
│  │  ├───────────────┼─────────────┼─────────────────────────┤  │    │
│  │  │ games         │ Игровые сессии │ ~1KB per game        │  │    │
│  │  │ game_moves    │ История ходов   │ ~50B per move        │  │    │
│  │  │ statistics    │ Статистика игрока│ ~500B per level     │  │    │
│  │  │ achievements  │ Достижения      │ ~200B per achievement│  │    │
│  │  │ daily_stats   │ Ежедневная стат.│ ~100B per day        │  │    │
│  │  │ puzzles_cache │ Кэш пазлов      │ ~2KB per puzzle      │  │    │
│  │  │ migrations    │ Версии схемы    │ ~100B per migration  │  │    │
│  │  └───────────────┴─────────────┴─────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                     ASYNC STORAGE                           │    │
│  │  ┌───────────────┬─────────────┬─────────────────────────┐  │    │
│  │  │      Key      │    Purpose  │       Content           │  │    │
│  │  ├───────────────┼─────────────┼─────────────────────────┤  │    │
│  │  │ user_settings │ Настройки польз│ Theme, language, etc. │  │    │
│  │  │ app_config    │ Конфигурация   │ First run, version    │  │    │
│  │  │ onboarding    │ Обучение       │ Tutorial progress     │  │    │
│  │  │ permissions   │ Разрешения     │ Analytics, notifications│ │    │
│  │  └───────────────┴─────────────┴─────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                        MMKV CACHE                           │    │
│  │  ┌───────────────┬─────────────┬─────────────────────────┐  │    │
│  │  │      Key      │    Purpose  │       TTL               │  │    │
│  │  ├───────────────┼─────────────┼─────────────────────────┤  │    │
│  │  │ current_game  │ Активная игра  │ Session                │  │    │
│  │  │ ui_state      │ Состояние UI   │ Session                │  │    │
│  │  │ temp_data     │ Временные данные│ 1 hour                │  │    │
│  │  │ api_cache     │ Кэш API (будущее)│ 5 minutes           │  │    │
│  │  └───────────────┴─────────────┴─────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ SQLite Database Schema

### 1. Таблица games (Игровые сессии)

```sql
-- Основная таблица для хранения игр
CREATE TABLE games (
    -- Основные поля
    id                  TEXT PRIMARY KEY,           -- UUID игры
    difficulty          TEXT NOT NULL,              -- 'easy'|'medium'|'hard'|'expert'
    status              TEXT NOT NULL DEFAULT 'active', -- 'active'|'completed'|'paused'|'abandoned'

    -- Игровые данные
    original_grid       TEXT NOT NULL,              -- JSON: исходная сетка 9x9
    current_grid        TEXT NOT NULL,              -- JSON: текущее состояние 9x9
    solution_grid       TEXT,                       -- JSON: решение (опционально)
    notes_grid          TEXT,                       -- JSON: заметки игрока 9x9x9

    -- Временные метки
    created_at          INTEGER NOT NULL,           -- Unix timestamp создания
    started_at          INTEGER,                    -- Unix timestamp начала игры
    completed_at        INTEGER,                    -- Unix timestamp завершения
    last_played_at      INTEGER NOT NULL,           -- Unix timestamp последней игры

    -- Игровая статистика
    play_time_seconds   INTEGER NOT NULL DEFAULT 0, -- Время игры в секундах
    pause_time_seconds  INTEGER NOT NULL DEFAULT 0, -- Время на паузе
    total_moves         INTEGER NOT NULL DEFAULT 0, -- Общее количество ходов
    hints_used          INTEGER NOT NULL DEFAULT 0, -- Использовано подсказок
    errors_count        INTEGER NOT NULL DEFAULT 0, -- Количество ошибок

    -- Метаданные
    game_version        TEXT NOT NULL DEFAULT '1.0', -- Версия игрового движка
    device_id           TEXT,                       -- Идентификатор устройства
    sync_status         TEXT DEFAULT 'local',       -- 'local'|'synced'|'conflict'

    -- Дополнительные данные
    metadata            TEXT,                       -- JSON: дополнительные данные

    -- Ограничения
    CHECK (difficulty IN ('easy', 'medium', 'hard', 'expert')),
    CHECK (status IN ('active', 'completed', 'paused', 'abandoned')),
    CHECK (play_time_seconds >= 0),
    CHECK (hints_used >= 0),
    CHECK (errors_count >= 0),
    CHECK (total_moves >= 0)
);

-- Индексы для производительности
CREATE INDEX idx_games_difficulty ON games(difficulty);
CREATE INDEX idx_games_status ON games(status);
CREATE INDEX idx_games_created_at ON games(created_at DESC);
CREATE INDEX idx_games_last_played ON games(last_played_at DESC);
CREATE INDEX idx_games_completed ON games(completed_at DESC);
CREATE INDEX idx_games_lookup ON games(status, difficulty, last_played_at DESC);
```

### 2. Таблица game_moves (История ходов)

```sql
-- История всех ходов игрока для отмены и анализа
CREATE TABLE game_moves (
    -- Основные поля
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    game_id             TEXT NOT NULL,              -- Ссылка на games.id
    move_number         INTEGER NOT NULL,           -- Порядковый номер хода

    -- Данные хода
    row                 INTEGER NOT NULL,           -- Строка (0-8)
    col                 INTEGER NOT NULL,           -- Столбец (0-8)
    old_value           INTEGER NOT NULL,           -- Предыдущее значение (0-9)
    new_value           INTEGER NOT NULL,           -- Новое значение (0-9)

    -- Тип операции
    move_type           TEXT NOT NULL,              -- 'user'|'hint'|'auto'|'undo'
    is_notes            INTEGER NOT NULL DEFAULT 0, -- Это заметки (0|1)
    notes_added         TEXT,                       -- JSON: добавленные заметки
    notes_removed       TEXT,                       -- JSON: удаленные заметки

    -- Временные метки
    timestamp           INTEGER NOT NULL,           -- Unix timestamp хода
    game_time_seconds   INTEGER NOT NULL,           -- Время игры на момент хода

    -- Валидация
    is_valid            INTEGER NOT NULL DEFAULT 1, -- Корректный ход (0|1)
    conflicts           TEXT,                       -- JSON: конфликты если есть

    -- Метаданные
    device_info         TEXT,                       -- JSON: информация об устройстве

    -- Ограничения
    FOREIGN KEY (game_id) REFERENCES games(id) ON DELETE CASCADE,
    CHECK (row >= 0 AND row <= 8),
    CHECK (col >= 0 AND col <= 8),
    CHECK (old_value >= 0 AND old_value <= 9),
    CHECK (new_value >= 0 AND new_value <= 9),
    CHECK (move_type IN ('user', 'hint', 'auto', 'undo')),
    CHECK (is_notes IN (0, 1)),
    CHECK (is_valid IN (0, 1))
);

-- Индексы
CREATE INDEX idx_moves_game_id ON game_moves(game_id);
CREATE INDEX idx_moves_game_move ON game_moves(game_id, move_number);
CREATE INDEX idx_moves_timestamp ON game_moves(timestamp DESC);
CREATE INDEX idx_moves_position ON game_moves(row, col);
```

### 3. Таблица statistics (Статистика игрока)

```sql
-- Общая статистика игрока по уровням сложности
CREATE TABLE statistics (
    -- Основные поля
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    difficulty          TEXT NOT NULL,              -- Уровень сложности

    -- Статистика игр
    games_started       INTEGER NOT NULL DEFAULT 0, -- Начато игр
    games_completed     INTEGER NOT NULL DEFAULT 0, -- Завершено игр
    games_abandoned     INTEGER NOT NULL DEFAULT 0, -- Брошено игр

    -- Временная статистика (в секундах)
    total_play_time     INTEGER NOT NULL DEFAULT 0, -- Общее время игры
    best_time           INTEGER,                    -- Лучшее время (завершенных)
    average_time        INTEGER,                    -- Среднее время
    worst_time          INTEGER,                    -- Худшее время

    -- Статистика ходов
    total_moves         INTEGER NOT NULL DEFAULT 0, -- Общее количество ходов
    average_moves       REAL,                       -- Среднее количество ходов
    best_moves          INTEGER,                    -- Минимум ходов

    -- Статистика помощи
    total_hints_used    INTEGER NOT NULL DEFAULT 0, -- Всего подсказок
    total_errors        INTEGER NOT NULL DEFAULT 0, -- Всего ошибок

    -- Серии
    current_streak      INTEGER NOT NULL DEFAULT 0, -- Текущая серия побед
    longest_streak      INTEGER NOT NULL DEFAULT 0, -- Лучшая серия
    current_loss_streak INTEGER NOT NULL DEFAULT 0, -- Текущая серия поражений

    -- Достижения
    perfect_games       INTEGER NOT NULL DEFAULT 0, -- Игры без ошибок
    hint_free_games     INTEGER NOT NULL DEFAULT 0, -- Игры без подсказок
    speed_records       INTEGER NOT NULL DEFAULT 0, -- Рекорды скорости

    -- Временные метки
    first_game_at       INTEGER,                    -- Первая игра
    last_game_at        INTEGER,                    -- Последняя игра
    last_updated_at     INTEGER NOT NULL,           -- Последнее обновление

    -- Метаданные
    version             TEXT NOT NULL DEFAULT '1.0', -- Версия статистики

    -- Ограничения
    CHECK (difficulty IN ('easy', 'medium', 'hard', 'expert')),
    CHECK (games_completed <= games_started),
    CHECK (current_streak >= 0),
    CHECK (longest_streak >= 0),
    UNIQUE(difficulty)
);

-- Индексы
CREATE INDEX idx_stats_difficulty ON statistics(difficulty);
CREATE INDEX idx_stats_last_game ON statistics(last_game_at DESC);
```

### 4. Таблица achievements (Достижения)

```sql
-- Система достижений игрока
CREATE TABLE achievements (
    -- Основные поля
    id                  TEXT PRIMARY KEY,           -- Уникальный код достижения
    category            TEXT NOT NULL,              -- Категория достижения
    title               TEXT NOT NULL,              -- Название достижения
    description         TEXT NOT NULL,              -- Описание

    -- Прогресс
    is_unlocked         INTEGER NOT NULL DEFAULT 0, -- Разблокировано (0|1)
    progress_current    INTEGER NOT NULL DEFAULT 0, -- Текущий прогресс
    progress_target     INTEGER NOT NULL,           -- Целевое значение

    -- Типы достижений
    achievement_type    TEXT NOT NULL,              -- 'single'|'progress'|'milestone'
    difficulty_specific TEXT,                       -- Привязка к сложности

    -- Награды
    reward_type         TEXT,                       -- Тип награды
    reward_value        TEXT,                       -- Значение награды (JSON)

    -- Временные метки
    unlocked_at         INTEGER,                    -- Время разблокировки
    first_progress_at   INTEGER,                    -- Первый прогресс
    last_progress_at    INTEGER,                    -- Последний прогресс

    -- Метаданные
    icon                TEXT,                       -- Иконка достижения
    rarity              TEXT NOT NULL DEFAULT 'common', -- Редкость
    points              INTEGER NOT NULL DEFAULT 0, -- Очки за достижение
    hidden              INTEGER NOT NULL DEFAULT 0, -- Скрытое достижение

    -- Ограничения
    CHECK (is_unlocked IN (0, 1)),
    CHECK (progress_current >= 0),
    CHECK (progress_target > 0),
    CHECK (progress_current <= progress_target),
    CHECK (achievement_type IN ('single', 'progress', 'milestone')),
    CHECK (rarity IN ('common', 'rare', 'epic', 'legendary')),
    CHECK (hidden IN (0, 1))
);

-- Индексы
CREATE INDEX idx_achievements_category ON achievements(category);
CREATE INDEX idx_achievements_unlocked ON achievements(is_unlocked, unlocked_at DESC);
CREATE INDEX idx_achievements_progress ON achievements(achievement_type, progress_current);
CREATE INDEX idx_achievements_difficulty ON achievements(difficulty_specific);
```

### 5. Таблица daily_stats (Ежедневная статистика)

```sql
-- Ежедневная статистика для трекинга активности
CREATE TABLE daily_stats (
    -- Основные поля
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    date                TEXT NOT NULL,              -- Дата в формате YYYY-MM-DD

    -- Статистика дня
    games_played        INTEGER NOT NULL DEFAULT 0, -- Сыграно игр
    games_completed     INTEGER NOT NULL DEFAULT 0, -- Завершено игр
    total_play_time     INTEGER NOT NULL DEFAULT 0, -- Время игры (секунды)
    total_moves         INTEGER NOT NULL DEFAULT 0, -- Всего ходов
    hints_used          INTEGER NOT NULL DEFAULT 0, -- Подсказок использовано
    errors_made         INTEGER NOT NULL DEFAULT 0, -- Ошибок сделано

    -- Лучшие результаты дня
    best_time_easy      INTEGER,                    -- Лучшее время легкий
    best_time_medium    INTEGER,                    -- Лучшее время средний
    best_time_hard      INTEGER,                    -- Лучшее время сложный
    best_time_expert    INTEGER,                    -- Лучшее время экспертный

    -- Достижения дня
    achievements_unlocked INTEGER NOT NULL DEFAULT 0, -- Разблокировано достижений
    perfect_games       INTEGER NOT NULL DEFAULT 0, -- Идеальных игр

    -- Метаданные
    created_at          INTEGER NOT NULL,           -- Время создания записи
    updated_at          INTEGER NOT NULL,           -- Последнее обновление

    -- Ограничения
    UNIQUE(date),
    CHECK (games_completed <= games_played),
    CHECK (perfect_games <= games_completed)
);

-- Индексы
CREATE INDEX idx_daily_stats_date ON daily_stats(date DESC);
CREATE INDEX idx_daily_stats_games ON daily_stats(games_played DESC);
```

### 6. Таблица puzzles_cache (Кэш пазлов)

```sql
-- Кэш предгенерированных пазлов для быстрого старта
CREATE TABLE puzzles_cache (
    -- Основные поля
    id                  TEXT PRIMARY KEY,           -- UUID пазла
    difficulty          TEXT NOT NULL,              -- Уровень сложности

    -- Данные пазла
    puzzle_grid         TEXT NOT NULL,              -- JSON: исходный пазл
    solution_grid       TEXT NOT NULL,              -- JSON: решение

    -- Характеристики
    filled_cells        INTEGER NOT NULL,           -- Количество заполненных ячеек
    estimated_time      INTEGER,                    -- Оценочное время решения
    complexity_score    REAL,                       -- Оценка сложности

    -- Алгоритм генерации
    generator_algorithm TEXT NOT NULL,              -- Алгоритм генерации
    generator_seed      TEXT,                       -- Seed для воспроизводства
    generator_version   TEXT NOT NULL,              -- Версия генератора

    -- Использование
    used_count          INTEGER NOT NULL DEFAULT 0, -- Сколько раз использован
    last_used_at        INTEGER,                    -- Последнее использование

    -- Временные метки
    generated_at        INTEGER NOT NULL,           -- Время генерации
    expires_at          INTEGER,                    -- Время истечения (опционально)

    -- Ограничения
    CHECK (difficulty IN ('easy', 'medium', 'hard', 'expert')),
    CHECK (filled_cells >= 17 AND filled_cells <= 81), -- Минимум для валидного судоку
    CHECK (used_count >= 0)
);

-- Индексы
CREATE INDEX idx_puzzles_difficulty ON puzzles_cache(difficulty);
CREATE INDEX idx_puzzles_unused ON puzzles_cache(difficulty, used_count, generated_at);
CREATE INDEX idx_puzzles_expires ON puzzles_cache(expires_at);
```

### 7. Таблица migrations (Миграции схемы)

```sql
-- Отслеживание версий схемы базы данных
CREATE TABLE migrations (
    -- Основные поля
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    version             TEXT NOT NULL UNIQUE,       -- Версия миграции (e.g., "1.0.0")
    name                TEXT NOT NULL,              -- Название миграции

    -- SQL скрипты
    up_script           TEXT NOT NULL,              -- SQL для применения
    down_script         TEXT,                       -- SQL для отката

    -- Статус
    applied             INTEGER NOT NULL DEFAULT 0, -- Применена (0|1)
    applied_at          INTEGER,                    -- Время применения

    -- Метаданные
    description         TEXT,                       -- Описание изменений
    checksum            TEXT,                       -- Контрольная сумма скрипта

    CHECK (applied IN (0, 1))
);

-- Индексы
CREATE INDEX idx_migrations_version ON migrations(version);
CREATE INDEX idx_migrations_applied ON migrations(applied, applied_at);
```

---

## 🔍 Примеры запросов

### 1. Игровые запросы

```sql
-- Создание новой игры
INSERT INTO games (
    id, difficulty, original_grid, current_grid,
    created_at, last_played_at, game_version
) VALUES (
    ?, ?, ?, ?,
    strftime('%s', 'now'), strftime('%s', 'now'), '1.0'
);

-- Получение активных игр пользователя
SELECT
    id, difficulty, status,
    original_grid, current_grid,
    play_time_seconds, hints_used, errors_count,
    last_played_at
FROM games
WHERE status IN ('active', 'paused')
ORDER BY last_played_at DESC;

-- Завершение игры
UPDATE games
SET
    status = 'completed',
    completed_at = strftime('%s', 'now'),
    last_played_at = strftime('%s', 'now')
WHERE id = ?;

-- Топ 10 лучших времен по сложности
SELECT
    play_time_seconds,
    completed_at,
    hints_used,
    errors_count
FROM games
WHERE difficulty = ? AND status = 'completed'
ORDER BY play_time_seconds ASC
LIMIT 10;
```

### 2. Статистические запросы

```sql
-- Обновление статистики после завершения игры
INSERT OR REPLACE INTO statistics (
    difficulty, games_started, games_completed,
    total_play_time, total_moves, total_hints_used, total_errors,
    best_time, last_game_at, last_updated_at
) VALUES (
    ?,
    COALESCE((SELECT games_started FROM statistics WHERE difficulty = ?), 0) + 1,
    COALESCE((SELECT games_completed FROM statistics WHERE difficulty = ?), 0) + 1,
    COALESCE((SELECT total_play_time FROM statistics WHERE difficulty = ?), 0) + ?,
    COALESCE((SELECT total_moves FROM statistics WHERE difficulty = ?), 0) + ?,
    COALESCE((SELECT total_hints_used FROM statistics WHERE difficulty = ?), 0) + ?,
    COALESCE((SELECT total_errors FROM statistics WHERE difficulty = ?), 0) + ?,
    MIN(COALESCE((SELECT best_time FROM statistics WHERE difficulty = ?), 999999), ?),
    strftime('%s', 'now'),
    strftime('%s', 'now')
);

-- Получение общей статистики
SELECT
    difficulty,
    games_started,
    games_completed,
    ROUND(games_completed * 100.0 / NULLIF(games_started, 0), 1) as completion_rate,
    total_play_time,
    best_time,
    current_streak,
    longest_streak
FROM statistics
ORDER BY difficulty;

-- Статистика за последние 30 дней
SELECT
    COUNT(*) as games_count,
    SUM(play_time_seconds) as total_time,
    AVG(play_time_seconds) as avg_time,
    MIN(play_time_seconds) as best_time
FROM games
WHERE
    status = 'completed'
    AND completed_at > strftime('%s', 'now', '-30 days');
```

### 3. Запросы для достижений

```sql
-- Проверка прогресса достижения "Завершить N игр"
UPDATE achievements
SET
    progress_current = (
        SELECT COUNT(*)
        FROM games
        WHERE status = 'completed'
    ),
    last_progress_at = strftime('%s', 'now'),
    is_unlocked = CASE
        WHEN progress_current >= progress_target THEN 1
        ELSE is_unlocked
    END,
    unlocked_at = CASE
        WHEN progress_current >= progress_target AND unlocked_at IS NULL
        THEN strftime('%s', 'now')
        ELSE unlocked_at
    END
WHERE id = 'complete_games_10';

-- Получение разблокированных достижений
SELECT
    id, title, description, unlocked_at, points
FROM achievements
WHERE is_unlocked = 1
ORDER BY unlocked_at DESC;

-- Получение прогресса по категориям
SELECT
    category,
    COUNT(*) as total_achievements,
    SUM(is_unlocked) as unlocked_count,
    SUM(points * is_unlocked) as total_points
FROM achievements
GROUP BY category;
```

### 4. Аналитические запросы

```sql
-- Анализ сложности игр по дням недели
SELECT
    CASE strftime('%w', datetime(completed_at, 'unixepoch'))
        WHEN '0' THEN 'Воскресенье'
        WHEN '1' THEN 'Понедельник'
        WHEN '2' THEN 'Вторник'
        WHEN '3' THEN 'Среда'
        WHEN '4' THEN 'Четверг'
        WHEN '5' THEN 'Пятница'
        WHEN '6' THEN 'Суббота'
    END as day_of_week,
    difficulty,
    COUNT(*) as games_count,
    AVG(play_time_seconds) as avg_time
FROM games
WHERE status = 'completed'
GROUP BY strftime('%w', datetime(completed_at, 'unixepoch')), difficulty
ORDER BY strftime('%w', datetime(completed_at, 'unixepoch')), difficulty;

-- Прогресс игрока по времени
SELECT
    date(completed_at, 'unixepoch') as game_date,
    difficulty,
    COUNT(*) as games_completed,
    MIN(play_time_seconds) as best_time,
    AVG(play_time_seconds) as avg_time
FROM games
WHERE status = 'completed'
GROUP BY date(completed_at, 'unixepoch'), difficulty
ORDER BY game_date DESC, difficulty;

-- Топ дни по активности
SELECT
    date(last_played_at, 'unixepoch') as play_date,
    COUNT(*) as games_played,
    SUM(play_time_seconds) as total_time,
    COUNT(DISTINCT difficulty) as difficulties_played
FROM games
WHERE last_played_at > strftime('%s', 'now', '-90 days')
GROUP BY date(last_played_at, 'unixepoch')
ORDER BY games_played DESC, total_time DESC
LIMIT 10;
```

---

## 🗂️ AsyncStorage Schema

### Структура настроек пользователя

```typescript
// Ключ: 'user_settings'
interface UserSettings {
  // Внешний вид
  theme: 'light' | 'dark' | 'auto';
  colorScheme: 'blue' | 'green' | 'purple' | 'custom';
  customColors?: CustomColorScheme;

  // Локализация
  language: 'ru' | 'en' | 'auto';
  region: string;

  // Игровые настройки
  defaultDifficulty: 'easy' | 'medium' | 'hard' | 'expert';
  showHints: boolean;
  showErrors: boolean;
  autoCheckComplete: boolean;
  highlightSameNumbers: boolean;
  highlightRowColumnBox: boolean;
  showTimer: boolean;

  // Звук и вибрация
  soundEnabled: boolean;
  soundVolume: number; // 0-1
  vibrationEnabled: boolean;
  vibrationPattern: 'light' | 'medium' | 'strong';

  // Поведение
  pauseOnMinimize: boolean;
  keepScreenOn: boolean;
  confirmBeforeExit: boolean;
  autoSaveInterval: number; // seconds

  // Доступность
  fontSize: 'small' | 'medium' | 'large' | 'extra-large';
  highContrast: boolean;
  screenReaderSupport: boolean;

  // Версия настроек
  version: string;
  lastUpdated: number; // timestamp
}

// Ключ: 'app_config'
interface AppConfig {
  // Первый запуск
  isFirstLaunch: boolean;
  onboardingCompleted: boolean;
  tutorialCompleted: boolean;

  // Версии
  appVersion: string;
  lastKnownVersion: string;
  databaseVersion: string;

  // Разрешения
  analyticsEnabled: boolean;
  crashReportingEnabled: boolean;
  notificationsEnabled: boolean;

  // Функции
  featuresEnabled: {
    achievements: boolean;
    statistics: boolean;
    dailyChallenges: boolean;
    backup: boolean;
  };

  // Метаданные
  installDate: number;
  lastOpenDate: number;
  openCount: number;

  version: string;
}

// Ключ: 'onboarding_progress'
interface OnboardingProgress {
  currentStep: number;
  completedSteps: string[];
  skippedSteps: string[];
  totalSteps: number;

  // Детали обучения
  tutorialSeen: {
    gameBasics: boolean;
    hints: boolean;
    notes: boolean;
    settings: boolean;
    statistics: boolean;
  };

  // Первая игра
  firstGameCompleted: boolean;
  firstGameDifficulty?: string;
  firstGameTime?: number;

  version: string;
  lastUpdated: number;
}
```

---

## 💾 MMKV Cache Schema

### Структура кэша

```typescript
// Ключ: 'current_game_state'
interface CurrentGameState {
  gameId: string;
  lastAction: string;
  selectedCell: { row: number; col: number } | null;
  highlightedCells: Array<{ row: number; col: number }>;
  isNotesMode: boolean;

  // UI состояние
  showingHint: boolean;
  errorCells: Array<{ row: number; col: number }>;

  // Временные данные
  sessionStartTime: number;
  pauseStartTime?: number;

  version: string;
  timestamp: number;
}

// Ключ: 'ui_state'
interface UIState {
  // Модальные окна
  activeModal: string | null;
  modalProps: Record<string, any>;

  // Навигация
  currentScreen: string;
  screenHistory: string[];

  // Уведомления
  pendingToasts: Array<{
    id: string;
    message: string;
    type: 'success' | 'error' | 'info' | 'warning';
    timestamp: number;
  }>;

  // Состояние загрузки
  loadingStates: Record<string, boolean>;

  timestamp: number;
}

// Ключ: 'temp_data_*' (с TTL)
interface TempData {
  key: string;
  data: any;
  expiresAt: number;
  type: 'puzzle' | 'api_response' | 'user_session' | 'other';
}
```

---

## 🔄 Database Migrations

### Система миграций

```typescript
// Стратегия миграций
interface Migration {
  version: string;
  name: string;
  upScript: string;
  downScript?: string;
  description: string;
}

// Миграция 1.0.0 -> 1.1.0
const migration_1_1_0: Migration = {
  version: '1.1.0',
  name: 'add_notes_support',
  description: 'Добавляет поддержку заметок в ячейках',
  upScript: `
    -- Добавляем колонку для заметок
    ALTER TABLE games ADD COLUMN notes_grid TEXT;

    -- Добавляем индекс для производительности
    CREATE INDEX idx_games_notes ON games(notes_grid)
    WHERE notes_grid IS NOT NULL;

    -- Обновляем версию игр
    UPDATE games SET game_version = '1.1.0' WHERE game_version = '1.0.0';
  `,
  downScript: `
    -- Удаляем индекс
    DROP INDEX IF EXISTS idx_games_notes;

    -- Нельзя удалить колонку в SQLite, поэтому создаем новую таблицу
    CREATE TABLE games_backup AS
    SELECT id, difficulty, status, original_grid, current_grid, solution_grid,
           created_at, started_at, completed_at, last_played_at,
           play_time_seconds, pause_time_seconds, total_moves,
           hints_used, errors_count, game_version, device_id,
           sync_status, metadata
    FROM games;

    DROP TABLE games;
    ALTER TABLE games_backup RENAME TO games;

    -- Восстанавливаем индексы
    CREATE INDEX idx_games_difficulty ON games(difficulty);
    CREATE INDEX idx_games_status ON games(status);
    -- ... другие индексы
  `
};

// Система применения миграций
class DatabaseMigrator {
  private db: SQLiteDatabase;

  constructor(database: SQLiteDatabase) {
    this.db = database;
  }

  async getCurrentVersion(): Promise<string> {
    try {
      const result = await this.db.executeSql(
        'SELECT version FROM migrations WHERE applied = 1 ORDER BY applied_at DESC LIMIT 1'
      );

      if (result[0].rows.length > 0) {
        return result[0].rows.item(0).version;
      }
    } catch (error) {
      // Таблица migrations не существует - это первый запуск
    }

    return '1.0.0';
  }

  async applyMigration(migration: Migration): Promise<void> {
    const transaction = async () => {
      // Записываем миграцию
      await this.db.executeSql(
        `INSERT INTO migrations (version, name, up_script, down_script, description, checksum)
         VALUES (?, ?, ?, ?, ?, ?)`,
        [
          migration.version,
          migration.name,
          migration.upScript,
          migration.downScript || null,
          migration.description,
          this.calculateChecksum(migration.upScript)
        ]
      );

      // Применяем SQL скрипт
      const statements = migration.upScript.split(';').filter(s => s.trim());
      for (const statement of statements) {
        if (statement.trim()) {
          await this.db.executeSql(statement);
        }
      }

      // Отмечаем как примененную
      await this.db.executeSql(
        'UPDATE migrations SET applied = 1, applied_at = ? WHERE version = ?',
        [Math.floor(Date.now() / 1000), migration.version]
      );
    };

    await this.db.transaction(transaction);
  }

  private calculateChecksum(script: string): string {
    // Простая контрольная сумма для валидации
    return Buffer.from(script).toString('base64').slice(0, 32);
  }
}
```

---

## 📊 Performance Optimization

### Индексы для производительности

```sql
-- Составные индексы для частых запросов
CREATE INDEX idx_games_user_query ON games(status, difficulty, last_played_at DESC);
CREATE INDEX idx_games_stats_query ON games(difficulty, status, completed_at DESC);
CREATE INDEX idx_moves_game_analysis ON game_moves(game_id, timestamp, move_type);

-- Частичные индексы для экономии места
CREATE INDEX idx_games_active ON games(last_played_at DESC)
WHERE status IN ('active', 'paused');

CREATE INDEX idx_games_completed ON games(play_time_seconds ASC)
WHERE status = 'completed';

CREATE INDEX idx_achievements_progress ON achievements(progress_current, progress_target)
WHERE is_unlocked = 0;

-- Covering индексы для избежания lookup'ов
CREATE INDEX idx_games_list_covering ON games(
    status, difficulty, last_played_at DESC
) INCLUDING (id, play_time_seconds, hints_used, errors_count);
```

### Оптимизация запросов

```sql
-- Использование EXPLAIN QUERY PLAN для анализа
EXPLAIN QUERY PLAN
SELECT g.id, g.difficulty, g.play_time_seconds,
       s.best_time, s.average_time
FROM games g
LEFT JOIN statistics s ON g.difficulty = s.difficulty
WHERE g.status = 'completed'
  AND g.completed_at > strftime('%s', 'now', '-30 days')
ORDER BY g.completed_at DESC;

-- Оптимизированные подзапросы с WINDOW functions
SELECT
    id,
    difficulty,
    play_time_seconds,
    ROW_NUMBER() OVER (
        PARTITION BY difficulty
        ORDER BY play_time_seconds ASC
    ) as rank_in_difficulty,
    PERCENT_RANK() OVER (
        PARTITION BY difficulty
        ORDER BY play_time_seconds ASC
    ) as percentile
FROM games
WHERE status = 'completed';

-- Пагинация с LIMIT и OFFSET
SELECT id, difficulty, last_played_at, play_time_seconds
FROM games
WHERE status IN ('active', 'paused')
ORDER BY last_played_at DESC
LIMIT ? OFFSET ?;
```

---

## 💿 Backup & Recovery

### Стратегия резервного копирования

```typescript
// Интерфейс для бэкапа
interface BackupService {
  createBackup(): Promise<BackupFile>;
  restoreBackup(backupFile: BackupFile): Promise<void>;
  validateBackup(backupFile: BackupFile): Promise<boolean>;
  getBackupInfo(): Promise<BackupInfo>;
}

// Структура бэкапа
interface BackupFile {
  version: string;
  timestamp: number;
  platform: string;

  // Данные
  games: any[];
  statistics: any[];
  achievements: any[];
  settings: any;

  // Метаданные
  checksum: string;
  compression: 'none' | 'gzip';
  encrypted: boolean;
}

// Реализация бэкапа
class DatabaseBackupService implements BackupService {
  private db: SQLiteDatabase;

  async createBackup(): Promise<BackupFile> {
    const backup: BackupFile = {
      version: await this.getAppVersion(),
      timestamp: Date.now(),
      platform: Platform.OS,
      compression: 'gzip',
      encrypted: false,
      checksum: '',

      // Экспорт данных
      games: await this.exportGames(),
      statistics: await this.exportStatistics(),
      achievements: await this.exportAchievements(),
      settings: await this.exportSettings(),
    };

    backup.checksum = await this.calculateChecksum(backup);

    return backup;
  }

  async restoreBackup(backupFile: BackupFile): Promise<void> {
    // Валидация
    const isValid = await this.validateBackup(backupFile);
    if (!isValid) {
      throw new Error('Invalid backup file');
    }

    // Создаем резервную копию текущей БД
    const currentBackup = await this.createBackup();

    try {
      await this.db.transaction(async () => {
        // Очищаем существующие данные
        await this.clearAllData();

        // Восстанавливаем данные
        await this.importGames(backupFile.games);
        await this.importStatistics(backupFile.statistics);
        await this.importAchievements(backupFile.achievements);
        await this.importSettings(backupFile.settings);
      });
    } catch (error) {
      // В случае ошибки восстанавливаем текущие данные
      await this.restoreBackup(currentBackup);
      throw error;
    }
  }

  private async exportGames(): Promise<any[]> {
    const result = await this.db.executeSql('SELECT * FROM games');
    const games = [];

    for (let i = 0; i < result[0].rows.length; i++) {
      games.push(result[0].rows.item(i));
    }

    return games;
  }

  private async calculateChecksum(backup: BackupFile): Promise<string> {
    const data = JSON.stringify({
      games: backup.games,
      statistics: backup.statistics,
      achievements: backup.achievements,
      settings: backup.settings
    });

    // Используем crypto для расчета SHA-256
    const hash = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(data));
    return Array.from(new Uint8Array(hash))
      .map(b => b.toString(16).padStart(2, '0'))
      .join('');
  }
}
```

### Автоматическое резервное копирование

```typescript
// Сервис автоматических бэкапов
class AutoBackupService {
  private backupService: BackupService;
  private storageService: CloudStorageService;

  async scheduleAutoBackup(): Promise<void> {
    // Проверяем настройки пользователя
    const settings = await this.getBackupSettings();
    if (!settings.autoBackupEnabled) return;

    // Проверяем время последнего бэкапа
    const lastBackup = await this.getLastBackupTime();
    const now = Date.now();
    const daysSinceLastBackup = (now - lastBackup) / (1000 * 60 * 60 * 24);

    if (daysSinceLastBackup >= settings.backupIntervalDays) {
      await this.performAutoBackup();
    }
  }

  private async performAutoBackup(): Promise<void> {
    try {
      // Создаем бэкап
      const backup = await this.backupService.createBackup();

      // Сохраняем локально
      await this.saveLocalBackup(backup);

      // Загружаем в облако (если настроено)
      if (await this.isCloudBackupEnabled()) {
        await this.storageService.uploadBackup(backup);
      }

      // Обновляем время последнего бэкапа
      await this.updateLastBackupTime(Date.now());

      // Уведомляем пользователя
      this.showBackupSuccessNotification();

    } catch (error) {
      console.error('Auto backup failed:', error);
      this.showBackupErrorNotification(error);
    }
  }
}
```

---

## 🔐 Security & Privacy

### Шифрование чувствительных данных

```typescript
// Сервис шифрования для чувствительных данных
class DataEncryptionService {
  private key: string;

  constructor() {
    this.key = this.getOrCreateEncryptionKey();
  }

  async encryptGameData(gameData: any): Promise<string> {
    const jsonData = JSON.stringify(gameData);
    const encrypted = await crypto.subtle.encrypt(
      { name: 'AES-GCM', iv: crypto.getRandomValues(new Uint8Array(12)) },
      await this.getKey(),
      new TextEncoder().encode(jsonData)
    );

    return Buffer.from(encrypted).toString('base64');
  }

  async decryptGameData(encryptedData: string): Promise<any> {
    const buffer = Buffer.from(encryptedData, 'base64');
    const decrypted = await crypto.subtle.decrypt(
      { name: 'AES-GCM', iv: buffer.slice(0, 12) },
      await this.getKey(),
      buffer.slice(12)
    );

    return JSON.parse(new TextDecoder().decode(decrypted));
  }

  private getOrCreateEncryptionKey(): string {
    // Генерируем или получаем ключ из безопасного хранилища
    return Keychain.getInternetCredentials('sudoku_encryption_key');
  }
}
```

### Защита от SQL инъекций

```typescript
// Безопасный репозиторий с параметризованными запросами
class SecureGameRepository {
  private db: SQLiteDatabase;

  async saveGame(game: GameEntity): Promise<void> {
    // Всегда используем параметризованные запросы
    const query = `
      INSERT OR REPLACE INTO games (
        id, difficulty, status, original_grid, current_grid,
        created_at, last_played_at, play_time_seconds,
        hints_used, errors_count, game_version
      ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    `;

    const params = [
      game.id,
      game.difficulty,
      game.status,
      JSON.stringify(game.originalGrid),
      JSON.stringify(game.currentGrid),
      game.createdAt,
      game.lastPlayedAt,
      game.playTimeSeconds,
      game.hintsUsed,
      game.errorsCount,
      game.gameVersion
    ];

    await this.db.executeSql(query, params);
  }

  async findGamesByDifficulty(difficulty: string): Promise<GameEntity[]> {
    // Валидация входных данных
    if (!this.isValidDifficulty(difficulty)) {
      throw new Error('Invalid difficulty level');
    }

    const query = `
      SELECT * FROM games
      WHERE difficulty = ? AND status IN ('active', 'paused')
      ORDER BY last_played_at DESC
    `;

    const result = await this.db.executeSql(query, [difficulty]);
    return this.mapResultToEntities(result[0]);
  }

  private isValidDifficulty(difficulty: string): boolean {
    return ['easy', 'medium', 'hard', 'expert'].includes(difficulty);
  }
}
```

---

## ✅ Критерии готовности схемы БД

**Схема базы данных считается готовой, когда:**

1. ✅ **Таблицы спроектированы** - все 7 основных таблиц с полями и ограничениями
2. ✅ **Индексы созданы** - оптимизация производительности запросов
3. ✅ **Связи определены** - внешние ключи и ссылочная целостность
4. ✅ **Миграции спланированы** - система версионирования схемы
5. ✅ **Запросы оптимизированы** - примеры эффективных SQL запросов
6. ✅ **Резервное копирование** - стратегия backup/restore
7. ✅ **Безопасность заложена** - защита от инъекций и шифрование
8. ✅ **Хранилища разделены** - SQLite + AsyncStorage + MMKV

### Метрики готовности

- **Таблицы**: 7/7 (100%)
- **Индексы**: 15+ индексов для производительности
- **Примеры запросов**: 20+ оптимизированных запросов
- **Типы данных**: Полная схема TypeScript интерфейсов
- **Миграции**: Готовая система версионирования
- **Безопасность**: Параметризованные запросы + шифрование

---

## 🚀 Следующие шаги

После утверждения схемы базы данных:

1. **2.2.5** - Определение паттернов проектирования
2. **3.1.1** - Настройка проекта и среды разработки
3. **3.1.2** - Создание базовой структуры проекта
4. **3.2.1** - Реализация алгоритма генерации судоку
5. **3.3.1** - Создание основных экранов приложения

---

**Последнее обновление**: 19 сентября 2025
**Статус**: ✅ Завершено